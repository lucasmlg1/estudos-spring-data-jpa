spring:
  application:
    name: jpa
  datasource:
    url: jdbc:postgresql://localhost:5432/bookstore-jpa
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  properties:
    hibernate:
      jdbc:
        lob.non_contextual_creation: true
